<h1 class = "mb-2 title" > Hi! </h1>
<h2 class = "mb-5 subtitle" > Select your favorite cafeteria to see the menu </h2>

<div class = "container-fluid text-center">
  <% @stores.each do |store| %>
    <div class="card m-3 d-inline-block">
      <div class="card-body">
        <h5 class="card-title"><%= store.name %></h5>
        <%= image_tag('dinner.jpg', class: "card-img-top my-2") %>
        <p class="card-text"><%= store.description %></p>
        <%= link_to 'Ver menú', store, class: "btn btn-primary d-block" %>
      </div>
    </div>
  <% end  %>
</div>
